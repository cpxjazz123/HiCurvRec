{
  "issue": 247,
  "task": "v27 备胎: Stage 1 ResMLP encoder (residual + LayerNorm)",
  "rule_invoked": "R38 (training regress) + R37 (rollback)",
  "verdict": "NO-GO",
  "reason": "v27 RQ-VAE Stage 1 训练严重塌缩: codes=1/1/1/256 持续 20k 步未恢复 (v19 baseline 同期 step 6972 已 2/41/20); vq_loss 从 643 缓降到 407 但 codebook 完全不分化. ResMLP 残差 + LayerNorm 结构虽为 R36 框架级变更 (He2016+Ba2016), 但实际效果对 RQ-VAE Stage 1 L0/L1/L2 量化完全失效, 所有样本被分到同一 codeword.",
  "D1_spec": "v27 = Stage 1 encoder = ResMLP(input_proj → N ResBlock [LayerNorm → Linear → ReLU + residual] → final_proj → L2Norm), n_blocks=2. 其他 v19 设定保留 (USE_CURRICULUM=True, c_end=0.7, USE_M3_TRANSPORT=True, USE_M2_INTRINSIC=False, COMMITMENT_WEIGHT=1.0). R36 框架级变更: 仅改 encoder 几何结构, 不动 κ 不动 codebook.",
  "D2_implementation": "modules/encoder.py: 新增 ResMLP 类 (LayerNorm → Linear → ReLU + skip connection, n_blocks=2). modules/rqvae.py: 加 encoder_type='mlp'|'resmlp' 开关 + resmlp_n_blocks 参数 + _build_encoder helper. train_rqvae_instruments.py: USE_RESIDUAL_ENCODER=True, OUT_DIR=rqvae_out_v27_resmlp. infer_sids_instruments.py: CKPT/OUT_NPY 切到 v27 路径, encoder_type='resmlp'. 新建 configs/decoder_instruments_hgrec_v27.gin + build_v27_sids_for_hgrec.py.",
  "D3_gate1_failure": "Gate 1 — Stage 1 训练健康: ❌ FAIL. step 872 codes=1/1/1/256 + vl=641 (v19 同期 vl=0.004); step 6972 同期 codes 仍 1/1/1 (v19 同期 2/41/20); step 20000 codes 仍 1/1/1 (v19 同期 ~6/145/75). ResMLP 输出分布与 codebook 初始化不兼容, KMeans 初始中心全部 fallback 到同一 cluster, 所有样本量化到单一 codeword.",
  "D4_root_cause": "D4 文献诊断: ResMLP 的 LayerNorm 在 init 时会把 hidden activations 强制归一到 unit variance, 导致 input_proj(768→512) → 2 ResBlock 后输出分布与原始 MLP 不同; KMeans 聚类初始化因 f范数归一化把所有 9922 item embedding 投影到接近正交方向, 256 个 KMeans centroid 被压缩到 1 个实际 cluster. 对比 v19 MLP: 纯 Linear(无 norm), 输出 f范数自然分布, KMeans 聚类可分到 256 个有效 centroid. 修复方向 (v28): (a) ResMLP 最后一层移除 LayerNorm (保持 raw Linear + residual); (b) ResMLP 加 scaling factor (e.g. init gain=√2 与 Kaiming-init 对齐); (c) 退回到纯 ResMLP (移除 LayerNorm, 保留 residual).",
  "evidence": {
    "log_path": "/home/wlia0047/hj82_scratch2/wenyu/claude_tmp/v27_stage1.log",
    "step_872_codes": "1/1/1/256",
    "step_6972_codes": "1/1/1/256 (vs v19 同期 2/41/20)",
    "step_20000_codes": "1/1/1/256 (vs v19 同期 ~6/145/75)",
    "vl_step872": 641.23,
    "vl_step20000": 405.36,
    "v19_vl_step988": 0.0039,
    "kill_step": 20000,
    "kill_rule": "R23 wrapper broken (codes stuck at 1/1/1) + R38 training regress"
  },
  "next_step": "v27 ResMLP encoder 全程塌缩, R36 框架级变更在 Stage 1 RQ-VAE 不适用. 备胎方向: (a) v28 = Stage 1 encoder 去掉 LayerNorm, 仅保留 residual (即 ResMLP-no-norm), 可能规避 f范数归一化对 KMeans 的破坏; (b) v29 = Stage 1 强化 decoder 几何 (hyperbolic decoder + L2 proj), 不动 encoder; (c) v30 = Stage 3 attention Q/K 走 expmap 双曲 (R36 曲率机制变更 + 路径未尝试). 优先 (a) v28 ResMLP-no-norm.",
  "git_status": "R50 revert pending, commit + push pending, not auto re-run per R50."
}