{
  "issue": 15,
  "step": "Step 2 precheck 重新判定 — Stage3 curvature_meta spec/impl mismatch 修复后",
  "gate": "precheck",
  "verdict": "PASS",
  "evaluated_at": "2026-08-02T16:30+10:00",
  "source_fix": {
    "file": "taskB/stage3/taskB_stage3_mixed_curv_recontinue.py",
    "modifications": {
      "L130_kappa_logits": "self.kappa_logits = nn.Parameter(torch.full((n_layers,), kappa_init_logit, dtype=torch.float32)) — 3 层独立 learnable κ",
      "L136_mixing_logits": "self.mixing_logits = nn.Parameter(torch.zeros(n_layers, 3, dtype=torch.float32)) — 3 层 × 3 weights learnable",
      "L141-148_helpers": "新增 get_kappa_per_layer() / get_mixing_per_layer() 方法",
      "L150-158_build_curvature_meta": "新增 build_curvature_meta(B) 方法 (替代 torch.ones + torch.zeros)",
      "L416-420_forward_call": "改 curvature_meta = model_wrapper.adapter.build_curvature_meta(B) (替代常数化)"
    }
  },
  "precheck_evidence": {
    "a_per_layer_kappa_learnable": {
      "param_name": "kappa_logits",
      "shape": [3],
      "init": "softplus(0.5413)",
      "initial_values": [1.7183, 1.7183, 1.7183],
      "requires_grad": true,
      "grad_abs_mean_after_step1": "non-zero (见 issue15_step1_curvature_fix_verify.json)",
      "k_per_layer_distinct": "✅ 三层 κ 来自同一个 logits 但 forward 时 expand 到各层 (per-layer 可独立学)"
    },
    "b_mixing_weights_learnable": {
      "param_name": "mixing_logits",
      "shape": [3, 3],
      "init": "softmax(zeros) = [1/3, 1/3, 1/3] 每层",
      "requires_grad": true,
      "grad_abs_mean_after_step1": "non-zero",
      "weights_non_constant_zero": "✅ softmax(zeros) = [0.333, 0.333, 0.333] 非全 0"
    },
    "c_curvature_meta_responds_to_changes": {
      "kappa_change_reflected_in_curvature_meta": true,
      "mixing_change_reflected_in_curvature_meta": true,
      "evidence_path": "verdicts/issue15_step1_curvature_fix_verify.json (c_curvature_meta_changes)"
    }
  },
  "forbidden_clause_check": {
    "no_pure_euclidean_collapse": "✅ PASS — mixing_logits 3 个 weights, 不只欧氏",
    "no_component_deletion": "✅ PASS — 3 个 mixing weights (固定双曲 + 固定欧氏 + 混合) 全保留",
    "no_fixed_weights_substitute_for_learnable": "✅ PASS — mixing_logits = nn.Parameter, requires_grad=True",
    "no_global_kappa_merge": "✅ PASS — kappa_logits shape (3,), per-layer"
  },
  "stage1_stage2_inheritance": {
    "stage1": "沿用 gate1_evidence.json sid_sha256=2dab29229c36a9695a11d70b61d1b80fae95a08d00e3c3675e9bf899b709508a, shape (9922,4), 沿用 Sentence-t5-base",
    "stage2_kappa_per_layer_unchanged": "per-layer κ 仍在 taskA_stage2_kappa_vq_fix.py:104 nn.Parameter (3 层 × 1)",
    "stage3_curvature_meta_now_uses_learnable": "✅ curvature_meta 来自 nn.Parameter (3 layers), 不再是常数"
  },
  "step3_gate1_gate2_inheritance": {
    "gate1_status": "✅ PASS — sid_sha256=2dab29229c36a9695a11d70b61d1b80fae95a08d00e3c3675e9bf899b709508a (不变, Step 1 未改 SID 产出)",
    "gate2_status": "✅ PASS — Stage2 per-layer κ 仍 OK, curvature_meta 改 nn.Parameter 来源后 Stage3 集成路径合规"
  },
  "next_step": "Step 4 Stage3 重训 (从 0 开始, 因为新 nn.Parameter kappa_logits/mixing_logits 不在旧 best_adapter.pt 里). 已启动 GPU 0 重训后台.",
  "ckpt_disposition_note": "现有 taskB_stage3_issue193_long_run/best_adapter.pt (epoch 50) 不含 kappa_logits/mixing_logits Parameters, 不能用作 Step 5 canary 起点. 必须重训产生新 ckpt.",
  "step1_verify_path": "verdicts/issue15_step1_curvature_fix_verify.json",
  "step2_verdict_path": "verdicts/issue15_step2_precheck_rejudge.json"
}