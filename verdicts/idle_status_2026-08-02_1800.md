# Idle Status — 2026-08-02 Tick 18:00+10

**触发**: R10 v2 (0 open issue + §16 空 + 无 owner 派工) + R16 (loop 定时 tick)
**Git**: 471c9c5 (#18) → dbb5b96 (#19) → a5c8f4b (idle) → 3b2a402 (#20) → b4feddc (#21)
**Open issues**: 0
**GPU**: 4× L40S 全部空闲 (util 0%, mem 0 MiB)

## 本 tick 完成动作 (R26 + R27 双满足)

| 阶段 | 动作 | 产物 / verdict |
|------|------|---------------|
| R16+R26 | 扫描发现 #21 OPEN → R22 立即 R18 NO-GO 闭环 | verdicts/issue21_r18_nogo_verdict.json |
| R26 | 调研 #47 公式不可定位 (glab 404 + 仓库无引用) | (诊断 + 推荐方向) |
| R27 | commit + push + close #21 | commit b4feddc, note_3633507441 |
| R26 | 调研 HG-Rec 公共 API 替代 #47 公式路径 | hrqvae_free_curv.py geodesic_distance_unified |

## #21 verdict 推荐方向 (下个 issue 选项)

**option_a (推荐)** — 用 HG-Rec 公共 API 替代 #47 公式:
- `HG-Rec/model/hrqvae_free_curv.py` 提供:
  - `FreeCurvVectorQuantization` 类: `theta_m = nn.Parameter(torch.zeros(M))` (per-layer κ), `kappa_m()` 返回 softplus(theta)
  - `FreeCurvResidualVectorQuantization` 类: Stage1 RQ-VAE 用的, 含 `get_codebook()` + `update_tau()`
  - `geodesic_distance_unified(x, y, kappa)` 函数: Poincaré ball metric 标准公式 (替代 #47)
- 任务A BoundedAdapter 只需: 加 kappa_logits (3,) nn.Parameter + 调用 geodesic_distance_unified 同步重校准 codebook
- 优点: 不依赖不可验证的 #47 公式, 复用 HG-Rec 已审计公共 API, precheck 修复可实现

**option_b** — 完全重写 BoundedAdapter + 自定义 Poincaré ball metric:
- 优点: 完全控制, 但需要 audit 自定义公式正确性

**option_c** — owner 在 issue comment 明确 #47 公式:
- 等 owner 解释, 但 #47 = 404 风险高

## 推荐新 issue 优先级

| 推荐 | 优先级 | 理由 |
|------|--------|------|
| [方向A precheck 修复 v2] 用 hrqvae_free_curv.geodesic_distance_unified + FreeCurvResidualVectorQuantization 替代 #47 公式, 在 taskA BoundedAdapter 加 kappa_logits (3,) nn.Parameter + strict=False 加载 #17 ckpt | 高 | 修复 #19 + #21 共同前置, 不修 #19 option_C 无法启动 |
| [方向B Gate3] 双变量组合: 解冻 T5 顶层 + per-layer κ init [0,2,4] | 中 | #18 + #20 verdict 推荐, 同时修信号链路 + κ 分化 |
| [方向B Gate3] option_D 扩 conditioner capacity | 低 | #18 verdict 备选方案 |

## 当前架构状态

- **方向A (taskA)** BoundedKappaScaleAdapter 缺 per-layer learnable κ (#19 + #21 双 blocked, 唯一进展 = 发现 fsq_quantizer.geodesic_distance_unified 公共 API)
- **方向B (taskB)** BoundedWeightedMixedCurvatureConditioner 有 kappa_logits (3,) nn.Parameter + mixing_logits (3,3) nn.Parameter (#15 修复), 但 conditioner 路径对称 → 三层 κ 同步架构缺陷
- **Stage3 val_R@10 最佳**: #17 = 0.0724 (方向A, R@10 < baseline 0.1020) / #193 = 0.058 (方向B long_run)
- **代码复用可行性**: taskA 修复可直接借鉴 taskB kappa_init_logit + softplus + strict=False 模式

## Stale _TRAINING_PID 清单

| 路径 | PID | 状态 |
|------|-----|------|
| taskB/stage3/taskB_stage3_issue18_option_b/_TRAINING_PID | 1175344 (terminated) | stale |
| taskB/stage3/taskB_stage3_issue20_option_c/_TRAINING_PID | 1206244 (terminated) | stale |

(下个 tick 按 R16 检查新 issue 派工, 当前 0 open issue → R10 v2 idle)